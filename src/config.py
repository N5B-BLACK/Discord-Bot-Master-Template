"""
ملف الإعدادات المركزي - إعدادات على مستوى البوت كامل (كل السيرفرات).
إعدادات كل سيرفر لحاله (رولات وقنوات) صارت مخزنة بقاعدة البيانات، مضبوطة عن طريق أمر /setup
(شوف utils/db.py).
"""

import os

from dotenv import load_dotenv

load_dotenv()

# ---------------------------------------------------------
# إعدادات أساسية
# ---------------------------------------------------------
TOKEN = os.getenv("DISCORD_TOKEN")
PREFIX = os.getenv("BOT_PREFIX", "!")

# Guild ID: معرف سيرفر واحد للتطوير/الاختبار السريع (لسرعة ظهور Slash Commands)
# اتركه فاضي بعد ما تخلص تطوير عشان الأوامر تتزامن على كل السيرفرات
GUILD_ID = int(os.getenv("GUILD_ID", 0)) or None

# ---------------------------------------------------------
# قاعدة البيانات (لتخزين إعدادات كل سيرفر - رولات وقنوات)
# ---------------------------------------------------------
MONGODB_URI = os.getenv("MONGODB_URI")

# ---------------------------------------------------------
# مفاتيح خدمات خارجية (AI عبر OpenRouter)
# ---------------------------------------------------------
OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
AI_MODEL = os.getenv("AI_MODEL", "openrouter/free")

# ---------------------------------------------------------
# تخصيص الرسائل (عدّلها حسب هوية كل عميل/سيرفر)
# ---------------------------------------------------------
BOT_NAME = os.getenv("BOT_NAME", "المساعد")
WELCOME_MESSAGE = os.getenv(
    "WELCOME_MESSAGE", "أهلاً فيك {member} بسيرفر {guild}! 🎉"
)
EMBED_COLOR = int(os.getenv("EMBED_COLOR", "0x5865F2"), 16)
